#include <stdio.h>

int main()
{
    int i, m = 0;
    
    for(i=1; i<=500; i++){
        if (i % 2 != 0 && i % 3 == 0 ){
            m = m + i;
        }
    }
    printf("A  soma de todos os números ímpares que são múltiplos de três: %d", m);
    return 0;
}
