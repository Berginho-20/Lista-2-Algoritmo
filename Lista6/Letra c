#include <stdio.h>

int main()
{
    int n, i = 0;
    printf("digite o valor de n: ");
    scanf("%d", &n);
    while(i <= 10){
        printf("%d x %d = %d\n", i, n, n*i);
        i = i + 1;  
    }
    
    return 0;
}
